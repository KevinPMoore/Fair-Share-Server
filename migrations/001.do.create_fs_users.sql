CREATE TABLE users (
    userid INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    username TEXT NOT NULL UNIQUE,
    password TEXT NOT NULL,
    adiministrator BOOLEAN NOT NULL DEFAULT FALSE
);